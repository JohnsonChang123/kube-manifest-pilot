(function () {
  'use strict';

  // Fill these values only after the corresponding public pages/accounts exist.
  // Keep ads disabled until the ad platform has approved the site and its privacy
  // requirements have been implemented. Core generator code never depends on ads.
  window.KUBE_MANIFEST_PILOT_CONFIG = Object.freeze({
    githubUrl: 'https://github.com/JohnsonChang123/kube-manifest-pilot',
    supportUrl: '',
    adsEnabled: false
  });
})();
